NRIN — ENGINEERING HANDOFF
Date: 2026-03-25
Repo: /Users/davidduncan/Desktop/nrin
Branch: main
Latest pushed commits:
- 9524f3a  match: align engine, presentation, and patient flow
- 767b9be  brain: update code map + command center after matcher + UI alignment

READ FIRST
1. project_brain/NRIN_COMMAND_CENTER.md
2. project_brain/NRIN_CODE_MAP.md
3. project_brain/NEXT_SESSION_BOOT.md
4. project_brain/NRIN_FILE_TREE.md
5. project_brain/matching/NRIN_MATCH_PRESENTATION_V1.md

NON-NEGOTIABLE EXECUTION RULES
- Use repo path: /Users/davidduncan/Desktop/nrin
- Terminal-driven surgical edits only
- Inspect before changing
- Verify every edit with grep
- Run npx tsc --noEmit after edits
- Do not guess file paths
- Do not redesign architecture casually
- buildPatientProfile.ts = input boundary
- buildMatchViewModel.ts = output boundary

CURRENT SYSTEM STATE
The patient flow is now coherent:
- Intake
- Matching
- Facility selection
- Prescreen
- Confirmation / indigent fork

New routes now exist and are committed:
- src/app/patient/prescreen/page.tsx
- src/app/patient/confirmation/page.tsx
- src/app/patient/indigent-path/page.tsx

MATCHER / PRESENTATION WORK COMPLETED
1. Program scoring
- src/lib/matching/scorePrograms.ts
- moved from flat overlap scoring to primary-vs-secondary weighting
- this fixed “IOP everywhere” style inflation

2. Match explanations
- src/lib/matching/buildMatchExplanation.ts
- now distinguishes:
  - primary level match
  - partial level-of-care alignment
  - exact insurance acceptance
  - likely private insurance compatibility

3. View model / presentation contract
- src/lib/matching/buildMatchViewModel.ts
- now acts as true presentation boundary
- added presentation.reasonPills
- top card pills now derive from strengths/reasons instead of a separate drifted logic path
- removed hidden truncation bug where strengths were sliced too early and downstream pills lost family/professional signals
- next-step language now varies by insurance certainty

4. Patient profile input translation
- src/lib/matching/buildPatientProfile.ts
- strengthened signal extraction from notes
- MAT now supported from stronger text cues
- insurance carrier inference from notes was introduced for exact carrier cues like Aetna
- desiredLevelsOfCare now computed once, not twice

5. Insurance scoring
- src/lib/matching/scoreInsurance.ts
- progressive certainty doctrine:
  - exact carrier match
  - likely insurance compatibility
  - self-pay / indigent fallback
- no longer a simplistic binary-only signal

6. Match orchestration
- src/lib/matching/matchPatientToFacilities.ts
- added light diversity pass to reduce same-city clustering at the top

7. Step 5 / intake UX
- src/app/patient/components/Step5LifeFit.tsx
- InsuranceStatusSection is mounted
- skip/reopen interaction works
- “Skip for now” no longer dead-ends the section

8. Card/detail surfaces
- src/components/patient/MatchCardStack.tsx
- src/components/patient/MatchDetailSheet.tsx
- duplicate top-card header removed
- card pills now reflect actual match strengths better
- detail sheet remains the deeper explanation surface

IMPORTANT ARCHITECTURE TRUTH
Active runtime layers:
- src/app/patient/*
- src/components/patient/*
- src/lib/matching/*

Non-active but likely intentional future extraction targets:
- src/components/matching/*
These files are zero-byte stubs and were confirmed empty both locally and in iCloud copy.
Do NOT delete casually.
Likely they were meant to become shared match presentation bridge components later.

VERY IMPORTANT PRODUCT DOCTRINE
Separate:
1. viability
- level of care
- detox
- funding / insurance plausibility
- key specialty fit

2. preference tie-breakers
- island / mountain / urban
- music / yoga
- privacy
- distance from home
- similar lifestyle fit preferences

Preference tie-breaker is NOT honestly implemented yet because facility-side structured signals do not yet exist for those concepts.
Do not fake that layer.

KNOWN LIMITATIONS / NEXT REAL BUILD
1. Life-fit tie-breaker is the next major feature
- current ties are expected when clinical + insurance + specialty fit are similar
- next step is to inspect what structured facility-side preference data actually exists
- then build a separate preference score layer
- do not stuff this into scoreSpecialties.ts blindly

2. Reason normalization still needs work
- some presentation ordering still relies on label text
- future cleanup should replace string matching with structured reason types / enums

3. Insurance certainty still needs deeper refinement
- exact carrier acceptance vs broad “major insurance” support is now conceptually separated
- but the system can still be improved once pre-screen ratification captures more exact data

4. Shared presentation extraction is unfinished
- current runtime still uses:
  - src/components/patient/MatchCardStack.tsx
  - src/components/patient/MatchDetailSheet.tsx
- future extraction into src/components/matching/* is plausible but should be intentional

TESTING / SANITY CHECKS TO RUN FIRST IN A NEW SESSION
1. Start app
- cd /Users/davidduncan/Desktop/nrin
- npm run dev

2. Verify these flows:
- Step 5 insurance question appears
- skip/reopen works
- top card pills reflect full relevant strengths
- detail sheet shows deeper reasons and cautions
- prescreen route works
- confirmation route works
- indigent path route works

3. Recommended scenario tests
A. Strong program + likely insurance + professional
- private insurance
- pilot / license / work language
- expect professional signal and likely-insurance behavior

B. Exact insurance + MAT + family
- explicit Aetna or Medicaid
- opioids / benzos / MAT text
- kids / family involvement text
- expect exact carrier acceptance if facility evidence exists
- expect MAT and family support to surface

C. No insurance / self-pay or indigent
- expect alternative funding-oriented next step, not fake insurance confidence

COMMANDS TO START A NEW SESSION CLEANLY
- cd /Users/davidduncan/Desktop/nrin
- git pull
- npm run dev

WHAT THE NEXT AI SHOULD DO FIRST
1. Read:
- project_brain/NRIN_COMMAND_CENTER.md
- project_brain/NRIN_CODE_MAP.md

2. Confirm understanding of:
- input boundary = buildPatientProfile.ts
- output boundary = buildMatchViewModel.ts
- active UI = src/components/patient/*
- active engine = src/lib/matching/*

3. Do not touch the dead/stub matching folder until explicitly asked:
- src/components/matching/*

NEXT BUILD TARGET
Build an honest preference tie-breaker layer only after inspecting available structured facility-side data.
Desired outcome:
- viability stays separate from preferences
- preferences reorder viable results without compromising integrity
- no fake island/mountain/music scoring unless evidence exists

If the next AI seems confused, tell it:
“Stop. Read NRIN_COMMAND_CENTER.md and NRIN_CODE_MAP.md, then inspect before editing.”

And here’s the short boot prompt version:

NRIN BOOT

Repo:
- /Users/davidduncan/Desktop/nrin

Read first:
- project_brain/NRIN_COMMAND_CENTER.md
- project_brain/NRIN_CODE_MAP.md

Rules:
- terminal-driven surgical edits only
- inspect before changing
- grep after edits
- npx tsc --noEmit after edits
- no guessing
- no broad rewrites

Architecture:
- buildPatientProfile.ts = input boundary
- buildMatchViewModel.ts = output boundary
- active runtime UI = src/components/patient/*
- active engine = src/lib/matching/*
- src/components/matching/* = zero-byte future/shared extraction stubs, not active runtime

Current state:
- matcher + presentation + patient flow aligned
- prescreen / confirmation / indigent-path routes committed
- progressive insurance certainty added
- primary-vs-secondary program scoring added
- card pills now reflect strengths
- Step 5 insurance section + skip/reopen fixed

Next build:
- inspect structured facility-side data for honest life-fit tie-breakers
- do not fake island/mountain/music/privacy scoring
- keep viability separate from preferences

If you want, I can also turn that into a single terminal command that writes project_brain/NRIN_ENGINEERING_HANDOFF.md and project_brain/NEXT_BUILD_TARGET.md.

Pasted markdown.md
File
actually update this engineering handoff:


Perfect—this is a strong handoff already. I’m going to upgrade it (not rewrite it) so it reflects everything we actually fixed and discovered today—especially the matcher/presentation alignment, insurance doctrine, and the UI drift issues.

Below is your updated engineering handoff (drop-in replacement).

# NRIN — ENGINEERING HANDOFF (UPDATED)

## Session Outcome (TRUE STATE)

This session completed the transition from:

→ demo-style matching + partial evidence  
→ to a **coherent, real-data, evidence-backed matching + presentation system**

Additionally, we resolved a critical architectural issue:

> MATCHER OUTPUT ≠ PRESENTATION (they were drifting)

By end of session:

- `/patient/matches` is fully wired to real `facility_intelligence`
- facility identity is correctly sourced from `facility_sites`
- explanation layer supports real evidence (snippet + URL)
- **presentation layer is now aligned with explanation layer (no more split logic)**
- **reason → strengths → pills pipeline is unified**
- insurance logic now supports **progressive certainty**
- intake → match → selection → prescreen → confirmation flow is coherent
- system is stable and pushed to `main`

This is no longer a prototype. It is a working system.

---

## CRITICAL BREAKTHROUGH (THIS SESSION)

The biggest issue discovered and fixed:

### ❌ Before:
- Explanation reasons were correct
- BUT presentation (top card pills) used separate logic
- Result:
  - pills missing (MAT, family, professional)
  - only “primary care level match” showing
  - UI lying / incomplete

### ✅ After:
- `presentation.reasonPills` now derives from **actual strengths (top reasons)**
- strengths are derived from normalized reasons
- **single source of truth established**

> This removed the “two worlds” problem between:
> - matcher truth
> - UI display

---

## WHAT CHANGED (REAL UPDATES)

### 1. View Model Became Canonical Presentation Boundary

File:
- `src/lib/matching/buildMatchViewModel.ts`

Key upgrades:

- introduced `strengths` as:

const strengths = reasons
.filter(r => r.label)
.slice(0, 4)


- `presentation.reasonPills` now:
- derives from `strengths`
- preserves priority signals:
  - primary care level
  - insurance
  - MAT
  - professional
  - family

- removed truncation bug where slicing broke downstream pills

- dynamic next-step messaging based on insurance certainty

✅ RULE NOW:
- ALL presentation shaping happens here
- UI does not invent logic

---

### 2. Explanation Layer = Evidence Bridge

File:
- `src/lib/matching/buildMatchExplanation.ts`

Now supports:

- insurance evidence
- detox evidence
- MAT evidence

Each includes:
- snippet
- sourceUrl
- sourceLabel

This file is now:

> matcher → human explanation → evidence bridge

---

### 3. Insurance Logic Upgraded (IMPORTANT)

File:
- `src/lib/matching/scoreInsurance.ts`

System now supports **3 levels of certainty**:

1. Exact carrier match  
 → “Accepts Aetna”

2. Likely compatibility  
 → “Likely accepts private insurance”

3. Fallback  
 → self-pay / funding path

AND:

- presentation reflects this difference
- next-step messaging reflects this difference

This is a **core product advantage**.

---

### 4. Patient Input → Signal Extraction Improved

File:
- `src/lib/matching/buildPatientProfile.ts`

Upgrades:

- stronger parsing of:
- insurance carriers (e.g. Aetna)
- MAT need from language
- professional signals
- family signals

- fixed duplicate `desiredLevelsOfCare` bug

---

### 5. Specialty Scoring Layer Active

File:
- `src/lib/matching/scoreSpecialties.ts`

Handles:

- MAT
- professional program
- family program

⚠️ IMPORTANT:
Signals only fire if:
- patient expresses need
- AND facility has signal

If not firing → investigate:
- facility intelligence signals
- not UI

---

### 6. Matching Orchestration Stabilized

File:
- `src/lib/matching/matchPatientToFacilities.ts`

Adds:

- deduplication by name+city
- light diversity (city spread)

Prevents:
- same network flooding top results

---

### 7. Step 5 Intake UX Fixed

File:
- `src/app/patient/components/Step5LifeFit.tsx`

Fixes:

- insurance question restored
- skip behavior now collapses section (not deletes)
- sections can reopen (state preserved)

---

### 8. UI Surfaces Cleaned

Files:
- `MatchCardStack.tsx`
- `MatchDetailSheet.tsx`

Fixes:

- duplicate header removed
- pills now reflect actual strengths
- explanation surface intact

---

## IMPORTANT ARCHITECTURE TRUTH (LOCK THIS IN)

### ACTIVE RUNTIME


src/app/patient/*
src/components/patient/*
src/lib/matching/*


### NOT ACTIVE (BUT INTENTIONAL)


src/components/matching/*


These files are:
- empty locally
- empty in iCloud
- NOT referenced anywhere

Conclusion:

> These were intended as future shared components (bridge layer)

DO NOT:
- delete blindly
- wire prematurely

---

## DATA MODEL TRUTH (CONFIRMED)

### facility_intelligence
- capabilities
- normalized signals
- evidence arrays

### facility_sites
- identity
- name
- website
- city

Correct model:

facility_sites → identity
facility_intelligence → capability + evidence


---

## PRODUCT DOCTRINE (CRITICAL)

### Separate:

#### 1. VIABILITY
- level of care
- detox
- insurance plausibility
- MAT / family / professional

#### 2. PREFERENCE (NOT BUILT YET)
- environment (mountain / beach / city)
- therapy types (music / yoga)
- lifestyle alignment

⚠️ DO NOT FAKE THIS LAYER

---

## CURRENT SYSTEM STATUS

### WORKING

- real matching
- real evidence
- aligned UI + engine
- intake → match → selection → prescreen → confirmation flow

### NOT COMPLETE

- life-fit tie breaker
- reason type normalization (still string-based in places)
- facility-side preference data layer

---

## TESTING PROTOCOL (MANDATORY NEXT SESSION)

1. Run app:

cd ~/Desktop/nrin
npm run dev


2. Test scenarios:

### A — Professional + Insurance
- pilot language
- private insurance

Expect:
- professional signal
- likely insurance

---

### B — MAT + Family + Carrier
- opioids / benzos
- kids / family
- Aetna

Expect:
- MAT
- family
- exact carrier (if facility supports)

---

### C — No Insurance
- no insurance

Expect:
- funding path
- NOT false confidence

---

## NEXT BUILD TARGET (VERY IMPORTANT)

### Build: LIFE-FIT TIE BREAKER

But ONLY after:

1. Inspecting real facility data
2. Identifying structured signals

Goal:

- keep viability separate
- use preferences only to reorder viable matches
- no hallucinated scoring

---

## DO NOT DO NEXT

- do NOT rewrite matcher
- do NOT rebuild crawler
- do NOT redesign UI randomly
- do NOT add fake preference signals

---

## IF NEXT AI GETS LOST

Say:

> “Stop. Read NRIN_COMMAND_CENTER.md and NRIN_CODE_MAP.md.  
> Then inspect buildMatchViewModel.ts before touching anything.”

---

## EXECUTIVE SUMMARY

NRIN now has:

- real-data matching
- evidence-backed explanations
- aligned presentation layer
- stable patient flow

The next phase is NOT intelligence.

The next phase is:

> **controlled expansion of decision quality (life-fit) without breaking trust.**

Confirm:
1. input boundary
2. output boundary
3. active UI layer
4. next build target
Do not proceed until confirmed.